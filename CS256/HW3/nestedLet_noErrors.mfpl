(let ( (x 5)
       (f1 (lambda (a)
             (let ( (y (+ a x))
                    (f2 (lambda (a b)
                          (let ( (z x) )
                            (print(* b (- a z)))
                           )
                         )
                     )
                  )
                 (f2 -100 a)
             ) 
            )
         )
        (y 50)
       )
   (if (f1 x) y 2)
 )

